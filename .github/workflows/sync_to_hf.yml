name: Sync to HF

on:
  schedule:
    - cron: '0 * * * *'

jobs:
  sync:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Sync to HF
      run: |
            git fetch hf
            git merge hf/main --allow-unrelated-histories --no-edit -m "🔄 [skip ci] 從 HF 同步更新內容"
