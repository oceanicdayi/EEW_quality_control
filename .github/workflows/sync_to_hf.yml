name: Sync to Hugging Face Hub

on:
  push:
    branches: [main] # 當 main 分支有更新時觸發
  workflow_dispatch: # 允許手動觸發

jobs:
  sync-to-hub:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    env:
      HF_SPACE: cwbdayi/EEW_quality_control
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
        with:
          fetch-depth: 0
          lfs: true

      - name: Push to hub
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
        run: |
          # 1. 確保 LFS 已初始化 (如果你有大型檔案)
          git lfs install
          
          # 2. 設定遠端 URL (包含 Token)
          # 注意：cwbdayi 是你的用戶名，EEW_quality_control 是 Space 名稱
          REMOTE_URL="https://cwbdayi:$HF_TOKEN@huggingface.co/spaces/cwbdayi/EEW_quality_control"
          
          # 3. 強制推送
          # 使用 --force 可以解決 (stale info) 或 (fetch first) 的衝突問題
          git push --force $REMOTE_URL main

      - name: Wait for HF Space deployment
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
        run: |
          API_URL="https://huggingface.co/api/spaces/${HF_SPACE}"

          echo "等待 Hugging Face Space 部署完成..."

          MAX_ATTEMPTS=60
          SLEEP_SECONDS=10
          ATTEMPT=0

          while [ $ATTEMPT -lt $MAX_ATTEMPTS ]; do
            ATTEMPT=$((ATTEMPT + 1))

            RESPONSE=$(curl -s -H "Authorization: Bearer $HF_TOKEN" "${API_URL}/runtime")
            STAGE=$(echo "$RESPONSE" | python3 -c "import sys,json; print(json.load(sys.stdin).get('stage','UNKNOWN'))" 2>/dev/null)
            if [ -z "$STAGE" ]; then
              echo "⚠️ 無法解析 API 回應: ${RESPONSE}"
              STAGE="UNKNOWN"
            fi

            echo "嘗試 ${ATTEMPT}/${MAX_ATTEMPTS}: 目前狀態 = ${STAGE}"

            if [ "$STAGE" = "RUNNING" ]; then
              echo "✅ Space 部署完成，狀態: RUNNING"
              break
            elif [ "$STAGE" = "RUNTIME_ERROR" ] || [ "$STAGE" = "BUILD_ERROR" ]; then
              echo "❌ Space 部署失敗，狀態: ${STAGE}"
              break
            fi

            sleep $SLEEP_SECONDS
          done

          if [ $ATTEMPT -ge $MAX_ATTEMPTS ] && [ "$STAGE" != "RUNNING" ]; then
            echo "⚠️ 超過最大等待時間，目前狀態: ${STAGE}"
          fi

      - name: Fetch HF deployment logs
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
        run: |
          LOG_DIR="hf_deploy_logs"
          TIMESTAMP=$(date -u '+%Y%m%d_%H%M%S')

          mkdir -p "$LOG_DIR"

          echo "📥 抓取 Hugging Face Space 建構日誌..."

          # 抓取 build logs (部署過程日誌)
          if ! curl -s -N \
            -H "Authorization: Bearer $HF_TOKEN" \
            "https://huggingface.co/api/spaces/${HF_SPACE}/logs/build" \
            > "${LOG_DIR}/build_${TIMESTAMP}.txt" 2>&1; then
            echo "⚠️ 抓取 build logs 失敗"
          fi

          # 抓取 run logs (執行日誌)
          # 使用 timeout 避免串流連線無限等待
          if ! timeout 30 curl -s -N \
            -H "Authorization: Bearer $HF_TOKEN" \
            "https://huggingface.co/api/spaces/${HF_SPACE}/logs/run" \
            > "${LOG_DIR}/run_${TIMESTAMP}.txt" 2>&1; then
            echo "⚠️ 抓取 run logs 失敗或逾時"
          fi

          echo "📄 日誌已儲存至 ${LOG_DIR}/"
          ls -la "$LOG_DIR/"

      - name: Commit and push logs to GitHub
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          git add hf_deploy_logs/

          # 檢查是否有新的變更需要提交
          if git diff --cached --quiet; then
            echo "沒有新的日誌變更需要提交"
          else
            git commit -m "[skip ci] 📋 新增 HF Space 部署日誌 ($(date -u '+%Y-%m-%d %H:%M:%S UTC'))"
            git push origin main
            echo "✅ 日誌已推送到 GitHub"
          fi
